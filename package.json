{
  "name": "groupchat",
  "version": "1.0.0",
  "description": "No-login group chat with persistent history, built on Cloudflare Workers + Durable Objects.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
